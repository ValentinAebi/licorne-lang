
object Test {

   fn clamp(x: Int, min: Int, max: Int) -> Int {
      val result: Int;
      if x <= min {
         result = min;
      } else if x >= max {
         return max;
      } else {
         result = x;
      };
      return result;
   }

}
