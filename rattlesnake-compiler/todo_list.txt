FIXME investigate support of conditions like seq1.size() == seq2.size()  (see failing example in testmain.rsn)
    Maybe use e-graphs (without equality saturation) to keep track of equalities and congruences, and apply smartcasts to e-graph nodes

TODO explicit function preconditions  (e.g.  fn zip[T, U](ls1: List[T], ls2: List[U] | ls1.size() == ls2.size()) -> List[Pair[T, U]] )

TODO nullable vs non-nullable types (here as well e-graphs may be useful)

TODO compilation to bytecode
