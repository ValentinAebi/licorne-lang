+----------------------------+
| Language design to-do list |
+----------------------------+

- Auxiliary constructors, or at least default initializers for class fields

- Split compilation pipeline to run separately on distinct classes/methods, for the phases where no cross-function synchronization is needed
- Parser: redesign and add error recovery mechanisms (the current implementation can crash if an LL1 conflict is reached, although no such bug is known currently)


