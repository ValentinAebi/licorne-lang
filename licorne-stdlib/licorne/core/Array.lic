package licorne.core;

class Array[T](public size: Cnt, initializer: fn (Idx(this)) -> T) : Indexed {

    pure fn get(i: Idx(this)) -> T {
        panic "stub, compiled to xALOAD"
    }

    fn set(i: Idx(this), elem: T) {
        panic "stub, compiled to xSTORE"
    }

}
