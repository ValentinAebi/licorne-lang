package licorne.collections;

interface Collection[+T] {
    pure fn size(this) -> Cnt

    fn map[U](this, f: fn (T) -> U) -> Collection[U]

    fn filter(this, pred: pure fn (T) -> Bool) -> Collection[T with pred(it)]

    fn foreach(this, f: fn (T) -> Unit)

}
