package licorne.collections;

interface Collection[+T] {
    pure fn size() -> Cnt

    fn map[U](f: fn (T) -> U) -> Collection[U]

    fn filter(pred: pure fn (T) -> Bool) -> Collection[T with pred(it)]

    fn foreach(f: fn (T) -> Unit)

}
