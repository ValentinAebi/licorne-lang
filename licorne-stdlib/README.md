# Licorne's standard library

The standard library is an early work in progress, and is not properly handled by the compiler yet, nor is it used by the tests.

Compile using `java -jar <path-to-compiler-jar> compile licorne/*`
