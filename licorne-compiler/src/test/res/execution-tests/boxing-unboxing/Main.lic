import licorne.collections.SinglyLinkedLists.*;


object Main {

    fn main() {
        val array = new Array(4, |2*it + 1|);
        val ls = listFromArray(array);
        println(ls.get(2) - 1);
    }

}
