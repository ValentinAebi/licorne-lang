
object Main {

    main fn run() {
        var s = 0;
        for (var i = 1; i <= 10; i += 1) {
            s += i;
        };
        println(s);
    }

    // stub FIXME remove once intrinsics / stdlib are in place
    fn println(a: Any){}

}
