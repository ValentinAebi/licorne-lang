
object Main {

    main fn run() {
        val sum = sumUpTo(10);
        println(sum);
    }

    fn sumUpTo(n: [0..]) -> [0..] {
        var s = 0;
        for (var i = 1; i <= n; i += 1) {
            s += i;
        };
        return s;
    }

}
