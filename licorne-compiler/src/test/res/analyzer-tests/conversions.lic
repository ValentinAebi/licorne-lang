
object Test {

   fn test(){
      val x = 0;
      val y = x as Int;      // OK
      val z = x as Bool;     //> E : impossible conversion: Int to Bool
      // TODO tests with Chars and Doubles once they are implemented
   }

}
